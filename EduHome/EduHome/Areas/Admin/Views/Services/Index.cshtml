@model List<Service>
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
               <div class="d-flex justify-content-between">
                    <h4 class="card-title">Services</h4>
                    <a class="btn btn-primary text-white mx-2 btn-rounded" asp-action="Create">Create</a>
               </div>
                <div class="table-responsive pt-3">
                    <table id="myServices" class="table table-bordered">
                        <thead>
                            <tr>

                                <th>
                                    Name
                                </th>
                                
                                   <th>
                                    Status
                                </th>

                                <th class="text-right">
                                    Actions
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Service item in Model)
                            {
                                <tr>

                                    <td>
                                        @item.Title
                                    </td>

                                    <td>
                                        @if(item.IsDeactive){
                                            <span  class="text-danger">Passiv</span>
                                        }
                                        else{
                                            <span class="text-success">Aktiv</span>

                                        }
                                    </td>
                                    
                                    <td>
                                        <div class="d-flex justify-content-end">
                                            <a asp-action="Detail" asp-route-id="@item.Id" style="width:120px" class="btn btn-outline-warning mx-2 btn-rounded">Detail</a>
                                            <a asp-action="Update" asp-route-id="@item.Id" style="width:120px" class="btn ou btn-outline-info  mx-2 btn-rounded">Update</a>
                                            @if (item.IsDeactive)
                                            {
                                                <a asp-action="Activity" asp-route-id="@item.Id" style="width:120px" class="btn btn-outline-success  mx-2 btn-rounded">Activate</a>
                                            }
                                            else
                                            {
                                                <a asp-action="Activity" asp-route-id="@item.Id" style="width:120px" class="btn btn-outline-danger mx-2 btn-rounded">Deactivate</a>

                                            }
                                            
                                        </div>
                                    </td>

                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script>
        $(document).ready(function () {
            $('#myServices').DataTable();
        });</script>
}

